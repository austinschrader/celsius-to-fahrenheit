function convertCelsiusToFahrenheit(number1) {
  return alert((promptResult * 1.8) + 32)
};

const promptResult = parseInt(prompt("Enter in what you want to convert to Fahrenheit"))
convertCelsiusToFahrenheit(0)